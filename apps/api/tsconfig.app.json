{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // File Resolution
    "rootDir": "src",
    "outDir": "dist",

    // For nodejs:
    "lib": ["esnext"],
    "types": ["node"]
    // and npm install -D @types/node
  },
  "include": ["src"],
  "exclude": ["**/*.test.ts"]
}
